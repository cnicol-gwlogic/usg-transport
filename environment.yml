name: usg-transport

channels:
  - conda-forge
  - defaults

dependencies:
  - appdirs
  - filelock
  - fprettify
  - jupytext
  - matplotlib
  - meson>=1.1.0
  - ninja
  - numpy
  - pip
  - pip:
      - git+https://github.com/modflowpy/flopy.git
      - git+https://github.com/modflowpy/pymake.git
      - modflow-devtools
  - pytest
  - pytest-cases
  - pytest-dotenv
  - pytest-order
  - pytest-xdist
  - flaky
